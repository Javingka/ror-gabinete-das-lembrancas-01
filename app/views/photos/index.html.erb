<div class="container bg-info">
  <p id="notice"><%= notice %></p>
</div>

<h1>Lista de imagenes</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <table class="table">
      <thead>
        <tr>
          <th>Picture</th>
          <th> <%= link_to 'Objetos', gabinete_objects_path, class: 'btn btn-default' %> </th>
          <th> <%= link_to 'Exposiçoes', exhibits_path, class: 'btn btn-default' %></th>
        </tr>
      </thead>
    
      <tbody>
        <% @photos.each do |photo| %>
          <% obj = GabineteObject.find_by(id: photo.gabinete_object_id) %>
          <% exp = Exhibit.find_by(id: photo.exhibit_id) %>
          <tr>
            <td><%= photo.picture %></td>
            <% if !obj.nil? %>
            <td><%= link_to obj.nome, gabinete_object_path(obj)  , class: 'btn btn-link' %> </td>
          <% else %>
            <td> </td>
          <% end %>
            <td><%= link_to_if !exp.nil?, exp.cidade, exhibit_path(exp) , class: 'btn btn-link'%> </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
<br>

